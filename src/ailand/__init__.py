"""aiLand v0 - XGBoost emulator of the ecLand land surface model.

Prototype reimplementation of the ec-land-db training notebook as a set of
scripts, structured so the v0 -> v1 gaps (see README) can be closed one at a time.
"""

__version__ = "0.1.0"
